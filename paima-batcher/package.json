{
  "name": "@paima/batcher",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx tsc --build tsconfig.build.json",
    "dev": "DOTENV_CONFIG_PATH=./.env.development node --require dotenv/config ./runtime/build/index.js",
    "start": "DOTENV_CONFIG_PATH=./.env.production node --require dotenv/config ./runtime/build/index.js"
  },
  "workspaces": [
    "./address-validator",
    "./batched-transaction-poster",
    "./tower-defense-validator",
    "./db",
    "./game-input-validator",
    "./runtime",
    "./utils",
    "./webserver"
  ],
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
