import { MatchExecutor, MatchExecutorData, RoundExecutor, RoundExecutorData } from "../types";
export declare function buildRoundExecutor(data: RoundExecutorData): Promise<RoundExecutor>;
export declare function buildMatchExecutor(data: MatchExecutorData): Promise<MatchExecutor>;
